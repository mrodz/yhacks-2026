import SwiftUI

struct Theme {
    static let primaryAction = Color.blue
    static let secondaryAction = Color.blue.opacity(0.1)
    static let background = Color(uiColor: .systemGroupedBackground)
}
